



```
ETL  跨数据库，跨实例，跨平台
1.选择落盘类型 mysql 、redis 、 ES 、 接口 。。。
2.mysql 
	使用配置索引（定义好数据库，表）
	定义字段含义
	定义落盘类型（insert 、 update、delete）

redis 
	使用配置索引
	定义数据类型
	定义落盘类型（set 、add 、 set 等）


接口
	使用配置索引（oauth 协议，token，sign 等）
	定义请求方式（GET、POST)
	定义 oauth 协议，
	定义参数形式
```

